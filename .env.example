# Environment Variables for Vite
# Docs: https://vite.dev/guide/env-and-mode

# VITE_ prefix: Required for client-side (browser) access, since this is meant to
# be a SPA no server will be available for env vars
# Vite only exposes variables with VITE_ prefix to prevent leaking secrets
# Access in code: import.meta.env.VITE_SERVER_ID

VITE_SERVER_ID=your_service_id

# Alternative 1: Build-time replacement (vite.config.ts)
# Use define option to inject variables at build time
# See: https://vite.dev/config/shared-options.html#define
# Example in vite.config.ts:
#   define: {
#     __APP_VERSION__: JSON.stringify('1.0.0'),
#   }

# Alternative 2: Public directory (no env needed)
# Place non-sensitive config in /public/config.json
# Fetch at runtime: fetch('/portfolio/config.json')
